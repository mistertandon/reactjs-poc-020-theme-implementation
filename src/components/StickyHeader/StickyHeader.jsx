import React from "react";
import "./StickyHeader.scss";

const StickyHeader = () => {
  return (
    <section className="sticky-header--container">
      Hello from StickyHeader
    </section>
  );
};

export default StickyHeader;
